{
  "name": "trip-planer",
  "private": true,
  "main": "backend/index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
